// admin_products.js
document.addEventListener("DOMContentLoaded", () => {
  const app = document.getElementById("app");
  if (app) {
    app.textContent = "商品管理画面（仮）: ここに商品一覧や編集UIが表示されます。";
  }
  console.log("admin_products.js loaded");
});
